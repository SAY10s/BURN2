{
  "dependencies": {
    "@dice-roller/rpg-dice-roller": "^5.5.1",
    "copyfiles": "^2.4.1"
  },
  "scripts": {
    "sync-types": "copyfiles -u 2 shared/types/*.* backend/src/shared/types && copyfiles -u 2 shared/types/*.* frontend/src/shared/types && copyfiles -u 2 shared/consts/*.* backend/src/shared/consts && copyfiles -u 2 shared/consts/*.* frontend/src/shared/consts && copyfiles -u 2 shared/helpers/*.* backend/src/shared/helpers && copyfiles -u 2 shared/helpers/*.* frontend/src/shared/helpers",
    "dev": "concurrently \"npm start --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.2.0",
    "prettier": "^3.6.2",
    "prettier-plugin-tailwindcss": "^0.6.14"
  }
}
